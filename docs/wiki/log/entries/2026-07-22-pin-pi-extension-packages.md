# 2026-07-22 — tooling — pin project Pi packages

Added `.pi/settings.json` with pinned `pi-subagents@0.35.1` and `pi-web-access@0.13.0` packages so trusted remote project sessions can install aligned extension capabilities.
