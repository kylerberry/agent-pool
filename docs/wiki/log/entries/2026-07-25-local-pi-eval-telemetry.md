# 2026-07-25 — development harness — collect local Pi eval telemetry

Added an auto-discovered project Pi extension that binds `local-craft-*` child sessions to trusted active `/goal` ledger state and records privacy-minimized runtime telemetry. The collector uses finalized Pi assistant messages as the authority for actual provider/model usage and cost, stores only per-run keyed, domain-separated prompt/system hashes with coarse length buckets and allowlisted tool lifecycle metadata, and excludes prompt, assistant, tool argument/result, environment, secret, and changed-file content. Goal completion now emits sanitized `telemetry-only` eval-candidate manifests without making telemetry availability a completion gate. Formal routing remains governed by the builder-first, tested-fixture, bare-with-tools, N=3 eval contract.
